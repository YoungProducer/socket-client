import { CreateChat as CreateChatComponent } from './Component';
import { container as createChatContainer } from './container';

export const CreateChat =
    createChatContainer(CreateChatComponent);
