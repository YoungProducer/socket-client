import { SendMessage as SendMessageComponent } from './Component';
import { container as sendMessageContainer } from './container';

export const SendMessage =
    sendMessageContainer(SendMessageComponent);
